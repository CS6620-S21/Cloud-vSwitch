{
  "name": "vswitch-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "engines": {
    "node": "14.15.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "firebase-admin": "^9.6.0",
    "helmet": "^4.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": ". .env && nodemon server.js",
    "prod": "pm2 start server.js --name vswitch-api",
    "lint": "eslint --cache *.js && prettier --write *.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^8.2.0",
    "eslint": "^7.23.0",
    "prettier": "^2.2.1"
  }
}
